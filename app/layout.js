export const metadata = {
  title: "DensaCash",
  description: "Fast loans in Kenya",
};

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  );
}
